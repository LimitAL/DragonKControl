# 断线恢复与配置保活验证

日期：2026-09-16  
设备：DragonK-XAR1500236  
应用版本：1.4（build 5）

## 官方 App 对照

对本机 `DragonKing.app` 的 CoreBluetooth 写入进行捕获后确认：固定输出模式约每 1.5 秒向 `AE01` 重发一次完整的 20 字节 `0x84` 控制包。文档、娱乐、专家模式的 `0x81`、`0x82`、`0x83` 配置只在切换或重连恢复时写入一次；重复刷新这些模式包会重新初始化设备调度并造成瞬时高功率。

连接时，官方 App 在 `AE02` 通知就绪后依次写入 `4E 00 01 01`、`81 00 00`、`82 00 00`、`83 00 00`，读取三档设备配置，然后恢复当前模式。当前实现采用相同顺序。

## 恢复策略

- 最后一次控制模式、三档自定义参数以及固定输出目标均保存到本机偏好。
- 扫描和重连不清空保存配置。
- `AE01` 写入与 `AE02` 通知均就绪后执行握手、读取三档，再恢复上次模式。
- 固定输出 `0x84` 每 1.5 秒刷新；三档模式包仅在切换或重连时写入一次。
- 界面显示通知数、断开数、自动恢复数、配置刷新数、协议查询数和最长通知间隔。
- Debug 构建支持通过 Xcode Scheme 环境变量 `DRAGONK_TEST_DISCONNECT_AFTER=<秒>` 注入一次 CoreBluetooth 断线；未设置时不生效，Release 构建不包含该入口。

## 实机结果

早期固定输出测试将目标设为 70/70，并在连接 5 秒时由 Debug 构建主动取消一次真实 CoreBluetooth 连接。应用记录到断开 1 次、自动恢复 1 次；重连后设备再次确认水泵 70、风扇 70，没有回到 42/42 待机值。

连续通知观察持续 15 分钟，共收到 1714 次通知，最大通知间隔 2.8 秒。期间两次断开均由测试中主动重启官方 App 引发，约 2 秒后恢复。后续 Xcode Run 验证中，应用重新启动后按保存状态恢复文档模式，水泵保持 42，持续收到 `49/4F` 与 `C0` 数据，没有再次落入待机。

三档模式的实机对照还确认：专家模式可以把三路冷核推到 100%，同时水泵实际功率仍为 42、风扇实际功率约为 37；`49` 内部通道值可为 100。由此确认物理水泵/风扇功率应读取 `C0[13]/[14]`，不能把 `49/4F` 内部通道值当作实际转速。
