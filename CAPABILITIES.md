# 官方软件与硬件能力盘点

依据：本机 `/Applications/DragonKing.app` 1.2.0 的本地化资源、原生 BLE 插件符号，以及真实设备 `DragonK-XAR1500236` 的 GATT 服务和通知。以下区分已在设备上验证的功能与软件仅显示的功能。

| 能力 | 证据 | 移植状态 |
| --- | --- | --- |
| 扫描、自动连接、信号强度、断开 | BLE 广播名与官方连接日志；真实设备可连接 | 已实现扫描、连接、RSSI、断开和可关闭的意外断开自动重连 |
| 水泵与风扇独立目标值 | 官方 `0x84` 控制包；独立改变第 7/12 字节，`AE02 4F/49` 分别回报目标 | 已实现双滑块、三档预设及目标回报 |
| 水泵与风扇运行数值 | `AE02 4F/49` 第 7 字节随运行变化，范围 0–100 | 已显示为“运行回报”；尚未校准为转速或真实功率 |
| 水泵与风扇运行趋势图 | 官方 App 包含 `WaterFanPowerLineChart` 组件；设备持续通知运行数值 | 已实现本地实时曲线，保留最近 240 个样本 |
| 温度、温差、功率控制模式 | 官方 App 本地化文案有“温度控制”“温差控制”“功率控制” | 尚未捕获对应控制包与状态机 |
| 文档、娱乐、专家模式；CPU 温度自动切换 | 官方 App 本地化文案、默认 `0x84` 自动模式日志 | 尚未移植自动切换逻辑；需要 CPU 温度读取和策略验证 |
| 左右冷凝板、水温、环境温度等读数 | 官方 App 本地化文案，`AE02 C0/49/4F` 含变化的数值 | 尚未确认每个字节与传感器的对应，保留原始诊断显示 |
| 蓝牙配置同步、设备/固件版本、恢复出厂设置 | 官方 App 本地化文案；设备还有 `AE03` 可写、`AE04` 通知、`AE05` 指示、`AE10` 可读 | 尚未识别安全的控制指令；不向这些特征写入 |
| 登录、云端配置上传/下载、多语言 | 官方 App 本地化资源和 Flutter 插件 | 属于官方服务能力；当前本地控制 App 未移植 |
| Mac 内置风扇 SMC 控制 | 官方 `local.framework` 中有 `SMCKit` 和内置风扇符号 | 与外置水冷器不同；当前未移植 |

硬件实际暴露 `AE00` 服务：`AE01` 与 `AE03` 为无响应写入，`AE02` 与 `AE04` 为通知，`AE05` 为指示，`AE10` 为可读/可写特征。`AE10` 读取值为 ASCII `dragonki`。当前 App 只向已验证的 `AE01` 写入官方控制包。

尝试在短时启动官方 App 时切换其保存的模式号 0–4：模式 4 会周期发送已验证的 `0x84` 包；模式 0–2 在启动时发送 `0x41` 旧协议包；模式 3 未出现明确的强度包。仅凭启动命令还不能确定温度、温差、功率和手动模式的安全参数，因此这些模式未移植。探测结束已恢复原偏好和 `42/42` 目标值。
