# 官方软件与硬件能力盘点

依据：本机 `/Applications/DragonKing.app` 1.2.0 的本地化资源、原生 BLE 插件符号，以及真实设备 `DragonK-XAR1500236` 的 GATT 服务和通知。以下区分已在设备上验证的功能与软件仅显示的功能。

| 能力 | 证据 | 移植状态 |
| --- | --- | --- |
| 扫描、自动连接、信号强度、断开 | BLE 广播名与官方连接日志；真实设备可连接 | 已实现扫描、连接、RSSI、断开和可关闭的意外断开自动重连 |
| 水泵与风扇独立目标值 | 官方 `0x84` 控制包；独立改变第 7/12 字节后真实设备行为变化，`4F[17]`/`49[12]` 回报目标 | 已作为高级固定输出保留；界面区分已发送目标与设备确认目标 |
| 水泵与风扇实际功率 | 厂商源码把 `C0[13]` 绑定为水泵功率；官方界面、原始通知与实机行为对照确认 `C0[14]` 为风扇功率 | 已在实时面板显示 `C0[13]/[14]`；与 `49/4F` 内部通道值明确分开 |
| 控制器内部运行趋势 | `AE02 4F/49` 第 7 字节随控制过程变化，且可与 `C0[13]/[14]` 出现显著不同 | 已显示为“内部通道/内部调度”，保留最近 240 个样本，不再标成物理设备输出 |
| 温度、温差、功率控制模式 | 厂商源码 `set_para`：模式包第 3 字节为 1/2/3，第 4 字节为对应目标值 | 已支持每档选择控制方式和目标值，并持久保存 |
| 文档、娱乐、专家模式 | 官方 App 实机写包与厂商小程序 `SET_docs/SET_movie/SET_expert` 源码；`0x81/0x82/0x83` 的字段布局相符 | 已移植三套设备端温差调度：温差 2/5/10、水泵固定 42、风扇自动 20–50、曲线系数 80 |
| 按 Mac CPU 温度自动选择三档 | 官方桌面 App 偏好中阈值为 0–60/60–70/70+；实际模式切换还受启动状态和温度采样影响 | 尚未自动切换；当前由用户明确选择三档，避免未经验证的模式跳变 |
| 左右冷凝板、水温、环境温度、三冷核功率 | 厂商页面绑定 `temp_Left/temp_Right/dis_temp_env/dis_temp_hot/Proportion_A/B/C`；`C0` 与 `49` 帧交叉出现相同字段 | 已解析 `C0[4/5]` 左右冷凝、`[8/9]` 环境/水温、`[10...12]` 三冷核功率、`[13/14]` 水泵/风扇功率，并显示设定温度 |
| 蓝牙配置同步、设备/固件版本、恢复出厂设置 | 官方 App 本地化文案；设备还有 `AE03` 可写、`AE04` 通知、`AE05` 指示、`AE10` 可读 | 尚未识别安全的控制指令；不向这些特征写入 |
| 登录、云端配置上传/下载、多语言 | 官方 App 本地化资源和 Flutter 插件 | 属于官方服务能力；当前本地控制 App 未移植 |
| Mac 内置风扇 SMC 控制 | 官方 `local.framework` 中有 `SMCKit` 和内置风扇符号 | 与外置水冷器不同；当前未移植 |
| 连接稳定性与配置恢复 | 官方 App 每约 1.5 秒重发 `0x84` 固定输出包；`0x81–0x83` 模式配置只在切换时写一次 | 固定输出按相同节奏刷新；三档温控仅在切换或重连后写一次。最后模式与目标都会持久保存并恢复 |
| 连接握手与三档配置读回 | 官方连接抓包依次出现 `4E 00 01 01`、`81/82/83 00 00`；厂商源码用模式包第 2 字节 0 读取参数 | 已在 AE02 通知就绪后按顺序查询，解析并在配置编辑器显示设备读回值，再恢复保存模式 |
| 待机、运行与当前状态 | 官方 App 显示“待机”时设备目标为 42/42；实测 42 是固件安全下限，目标高于 42 为运行 | 已提供“进入待机 42/42”“运行并应用当前设定”，并依据设备确认目标显示当前状态 |

硬件实际暴露 `AE00` 服务：`AE01` 与 `AE03` 为无响应写入，`AE02` 与 `AE04` 为通知，`AE05` 为指示，`AE10` 为可读/可写特征。`AE10` 读取值为 ASCII `dragonki`。当前 App 只向已验证的 `AE01` 写入官方控制包。

官方文档、娱乐、专家配置分别是 `81/82/83 00 03 01 <温差> 02 00 2A 00 00 01 32 14 00 50 00 00 00 00 00`。字段含义由小程序通用 `set_para` 编码器交叉确认：冷却逻辑 1 为温差控制；水泵逻辑 2 为固定 42；风扇逻辑 1 为线性曲线，最大 50、最小 20、系数 80。此前界面的 70% 与 100% 是 `0x84` 固定输出，并不等价于官方娱乐/专家模式。

待机实测排除了把目标写为 0 的做法：该固件会把 0 当作取消功率下限，风扇在 45 秒内升至 100%，并不会停机。官方界面显示“待机”时水泵、风扇目标均为 42，因此当前实现沿用 42/42 作为安全待机档；运行按钮重新应用用户当前设定。
官方 App 对 `0x84` 固定输出采用 1.5 秒刷新，而 `0x81–0x83` 模式配置只在切换时写一次。实测重复写专家模式会反复初始化调度并造成风扇瞬时冲到 100%，因此当前实现严格区分两者。扫描和重连不会清除上次模式或水泵/风扇目标，AE01 写入特征重新就绪后会立即恢复一次配置。
